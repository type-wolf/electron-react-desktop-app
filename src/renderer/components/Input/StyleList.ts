import type { InputListType } from 'renderer/types/components/Input';
import BaseInputStyle from './style/base';

const styleInputList: InputListType = {
    base: BaseInputStyle,
};

export default styleInputList;
